apiVersion: v1
kind: Service
metadata:
  name: payment-api-service
  namespace: {{ .Values.namespace }}

spec:
  type: ClusterIP

  selector:
    app: payment-api

  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
