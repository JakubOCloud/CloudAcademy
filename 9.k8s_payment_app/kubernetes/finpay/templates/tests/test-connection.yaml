apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "finpay.fullname" . }}-test-connection"
  labels:
    {{- include "finpay.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "finpay.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
