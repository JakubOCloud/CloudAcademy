apiVersion: v1
kind: Service
metadata:
  name: {{ include "finpay.fullname" . }}
  labels:
    {{- include "finpay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finpay.selectorLabels" . | nindent 4 }}
