apiVersion: v1
kind: Secret
metadata:
  name: payment-secret
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  DB_URL: "{{ .Values.secret.DB_URL }}"
  DB_USER: "{{ .Values.secret.DB_USER }}"
  DB_PASSWORD: "{{ .Values.secret.DB_PASSWORD }}"